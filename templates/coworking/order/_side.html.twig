
<h3 class="h6 mt-4">Commandes aujourd'hui</h3>
<div class="list-group">
    <div class="list-group-item list-group-item-action d-flex gap-3 py-3">
        <i class="bi bi-tags rounded-circle flex-shrink-0"></i>
        <div class="d-flex w-100 justify-content-between">
            <div>
            <p class="mb-0">{{ ordersToday|length }} {{ ordersToday|length == 1 ?  'commande' : 'commandes' }}</p>
            </div>
            <small class="opacity-50 text-nowrap">{{ amountTotal }} Ar</small>
        </div>
    </div>

    <div class="list-group-item list-group-item-action d-flex gap-3 py-3">
        <i class="bi bi-tags rounded-circle flex-shrink-0"></i>
        <div class="d-flex w-100 justify-content-between">
            <div>
            <p class="mb-0">{{ ordersBankedToday|length }} {{ ordersBankedToday|length == 1 ? 'payée' : 'payées' }}</p>
            </div>
            <small class="opacity-50 text-nowrap">{{ amount }} Ar</small>
        </div>
    </div>
</div>