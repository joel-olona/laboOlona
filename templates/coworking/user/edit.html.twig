{% extends 'base_coworking.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <h1 class="h3">Modifier un utilisateur</h1>

    {{ include('coworking/user/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex justify-content-end w-100">
        {{ include('coworking/user/_delete_form.html.twig') }}
        <a class="btn btn-primary px-5 rounded-pill" href="{{ path('app_user_index') }}">Retour à la liste</a>
    </div>

{% endblock %}
