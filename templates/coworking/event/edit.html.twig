{% extends 'base_coworking.html.twig' %}

{% block title %}Modifier la réservation{% endblock %}

{% block body %}
    <h1 class="h3">Modifier la réservation</h1>

    {{ include('coworking/event/_form.html.twig', {'button_label': 'Modifier'}) }}
    
    <div class="d-flex justify-content-end w-100">
        {{ include('coworking/event/_delete_form.html.twig') }}
    </div>
    <div>
        <a class="btn btn-primary px-5 rounded-pill" href="{{ path('app_event_index') }}"><i class="bi me-2 bi-arrow-left"></i>Retour à la liste</a>
    </div>

{% endblock %}
