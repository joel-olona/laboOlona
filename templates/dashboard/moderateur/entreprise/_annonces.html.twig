    
    <p class="mx-2">
    Votre recherche renvoie <strong>{{ result|length }}</strong> résultats.
    </p>
    <div class="navigation">
    {{ knp_pagination_render(annonces, 'parts/_pagination.html.twig') }}
    </div>
    <table class="table table-responsive">
        <thead>
            <tr>
                {# <th class="col-1">ID</th> #}
                <th class="col-3">Titre</th>
                <th class="col-1">Secteurs</th>
                {# <th class="col-1">Annonces</th> #}
                {# <th class="col-1">RDV</th> #}
                <th class="col-1">Status</th>
                <th class="col-2">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for annonce in annonces %}
                <tr>
                    {# <td class="id-column">{{ annonce.id }}</td> #}
                    <td>{{ annonce.titre }}</td>
                    <td>{{ annonce.secteur }}</td>
                    {# <td>
                        <a href="{{path('app_dashboard_moderateur_entreprises_annonces', { 'id': entreprise.id}) }}" class="btn btn-sm btn-info">
                            {{ annonce.jobListings|length }}
                        </a>
                    </td> #}
                    {# <td>{{ annonce.mettings|length }}</td> #}
                    <td>{{ annonce.status }}</td>
                    <td class="actions-col">
                        <a href="{{path('app_dashboard_moderateur_annonce_view', { 'id': annonce.id}) }}" class="btn btn-sm btn-info rounded-pill"><i class="bi mx-1 bi-eye"></i></a>
                        <a href="{{path('delete_annonce', { 'id': annonce.id}) }}" class="btn btn-sm btn-danger rounded-pill" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce type de contrat ?');"><i class="bi mx-1 bi-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>