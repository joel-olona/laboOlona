{% extends 'dashboard/base.html.twig' %}

{% block title %}Éditer le rendez-vous: {{ metting.id }}{% endblock %}

{% block body %}
    <h1>Éditer le rendez-vous: {{ metting.id }}</h1>

    {{ include('dashboard/moderateur/mettings_form.html.twig') }}

    <a href="{{ path('app_dashboard_moderateur_metting_show', {'id': metting.id}) }}">voir</a>
    <a href="{{ path('app_dashboard_moderateur_mettings') }}">Retour à la liste</a>
{% endblock %}