{% extends 'dashboard/base.html.twig' %}

{% block title %}Mes rendez-vous{% endblock %}

{% block body %}
    <div class="container mt-3">
        <h1 class="mb-4">Mes rendez-vous</h1>
        <hr>
        <div class="row">
            <div class="col-md-4">
                <a href="{{ path('app_dashboard_entreprise_annonces') }}" class="text-decoration-none">
                    <div class="card text-white bg-primary mb-3">
                        <div class="card-body">
                            <h2 class="card-title">{{ annonces|length }}</h2>
                            <p class="card-text">Offres d'emploi postées</p>
                        </div>
                    </div>
                </a>
            </div>

            <div class="col-md-4">
                <div class="card text-white bg-success mb-3">
                    <div class="card-body">
                        <h2 class="card-title">{{ applications|length }}</h2>
                        <p class="card-text">Candidatures reçues</p>
                    </div>
                </div>
            </div>
        
            <div class="col-md-4">
                <a href="{{ path('app_dashboard_entreprise_rendez_vous') }}" class="text-decoration-none">
                    <div class="card text-white bg-warning mb-3">
                        <div class="card-body">
                            <h2 class="card-title">{{ mettings|length }}</h2>
                            <p class="card-text">Rendez-vous programmés</p>
                        </div>
                    </div>
                </a>
            </div>
        </div>
        <div class="row my-4">
            <div class="col-md-12">
                <p class="text-muted">
                    <i class="bi bi-lightbulb"></i> Astuce : Utilisez les filtres ci-dessous pour affiner les résultats de vos candidatures. Cela peut vous aider à gérer plus efficacement vos offres d'emploi et les réponses reçues.
                </p>
            </div>
        </div>
        

        
    </div>
{% endblock %}