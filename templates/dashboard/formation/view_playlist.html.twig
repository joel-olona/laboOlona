
{% set base_template = app.user.type is same as(constant('App\\Entity\\User::ACCOUNT_MODERATEUR')) ? 'dashboard/base.html.twig' : 'dashboard/base_new.html.twig' %}

{% extends base_template %}

{% block title %}Formations{% endblock %}

{% block body %}
<div class="container mt-3">
    <h1 class="mb-4">{{ playlist.titre }}</h1>
    {% if app.user.type is same as(constant('App\\Entity\\User::ACCOUNT_MODERATEUR')) %}
    <a href="{{ path('app_dashboard_formation_playlist_new')}}" class="btn btn-warning my-3 rounded-pill px-5"><i class="h5 bi mx-1 bi-plus"></i>Playlist</a>
    <a href="{{ path('app_dashboard_formation_new')}}" class="btn btn-info my-3 rounded-pill px-5"><i class="h5 bi mx-1 bi-plus"></i>Vidéo</a>
    {% endif %}
    <div class="row">

    <div id="videos-container">
        {% include 'dashboard/formation/_videos.html.twig' with { videos: playlist.videos} %}
    </div>
    </div>
    <a href="javascript:history.back()" class="btn btn-success rounded-pill my-3 px-5"><i class="mx-2 bi bi-arrow-left"></i>Retour</a>
</div>

{% endblock %}
