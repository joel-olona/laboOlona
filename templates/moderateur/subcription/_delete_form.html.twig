<form method="post" action="{{ path('app_moderateur_subcription_delete', {'id': subcription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subcription.id) }}">
    <button class="btn btn-danger px-5 rounded-pill"><i class="bi me-2 bi-trash"></i>Supprimer</button>
</form>
