{% extends 'dashboard/base.html.twig' %}

{% block title %}Edit JobListing{% endblock %}

{% block body %}
<section class="p-0 m-0">
    <div class="container">
        <div class="row p-4 pb-0 pe-lg-0 pt-lg-5 align-items-center">
            <h1 class="h3">Modification annonce #{{ job_listing.id }}</h1>
            <hr>
            {{ include('moderateur/job_listing/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="d-flex justify-content-end w-100">
                {{ include('moderateur/job_listing/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
