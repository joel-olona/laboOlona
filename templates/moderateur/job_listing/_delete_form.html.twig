<form method="post" action="{{ path('app_moderateur_job_listing_delete', {'id': job_listing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_listing.id) }}">
<button class="btn btn-danger px-5 rounded-pill"><i class="bi me-2 bi-trash"></i>Supprimer</button>
</form>
