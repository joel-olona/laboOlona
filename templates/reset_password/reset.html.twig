{% extends 'base_v21.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
<section id="contact" class="my-5" style="">
    <div class="container my-5 d-flex flex-column justify-content-center">
        <div class="row my-4">
            <div class="col d-flex flex-column justify-content-center">
                <h1 class="text-center display-4">Réinitialiser mot de passe</h1>

                {{ form_start(resetForm, {'attr': {'class': 'col-md-6 mx-auto'}}) }}
                    {{ form_row(resetForm.plainPassword) }}
                    <button class="btn btn-primary">Réinitialiser le mot de passe</button>
                {{ form_end(resetForm) }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
{% block preview %}{% endblock %}
