{% extends 'base_v21.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
<section id="contact">
    <div class="shadow-custom col-12 col-sm-10 col-md-8 col-lg-8 p-2 p-sm-4 mx-auto">
        <h1 class="fs-4 fw-bolder text-center my-3 my-sm-5">
            <i class="bi bi-envelope-at text-warning me-2"></i>
            Réinitialiser mot de passe
        </h1>

        <p class="text-center my-4">Entrez votre nouveau mot de passe ci-dessous. Assurez-vous qu'il est sécurisé et unique pour ce site.</p>
        {{ form_start(resetForm, {'attr': {'class': 'col-md-6 mx-auto'}}) }}
            <div class="form-floating mb-3">
                {{ form_widget(resetForm.plainPassword.first) }}
                {{ form_label(resetForm.plainPassword.first) }}
                <div class="small fw-lighter mb-2">Votre mot de passe doit contenir au moins 8 caractères, dont une majuscule, une minuscule, un chiffre et un caractère spécial.</div>
            </div>
            <div class="form-floating mb-3">
                {{ form_widget(resetForm.plainPassword.second) }}
                {{ form_label(resetForm.plainPassword.second) }}
                <div class="small fw-lighter mb-2">Réentrez votre mot de passe pour confirmer.</div>
            </div>
            <button class="btn btn-primary px-5 btn-lg text-uppercase fw-bold my-4">Réinitialiser le mot de passe</button>
        {{ form_end(resetForm) }}
        <p class="text-center mt-3">Si vous avez des questions ou besoin d'aide, n'hésitez pas à contacter notre support technique.</p>
    </div>
</section>
{% endblock %}
{% block preview %}{% endblock %}
