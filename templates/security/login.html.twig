{# {% extends 'base.html.twig' %}

{% block body %}
  <section id="login" class="pt-5 " style="background-color: black;">
    <div class="container d-flex justify-content-center align-items-center">
      <div class="row w-100">
        <div class="col-md-6 text-center mx-auto d-none d-sm-block">
          <img src="https://tecdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/draw2.svg" class="img-fluid" alt="login olona talents" />
        </div>
        <div class="col-md-6 text-white">
          <h1 class="fs-3 mb-3 font-weight-normal">
            {{ meta_title() }}
          </h1>
          <p class="my-4 text-white">
          {{ 'app_login.not_member'|trans }}
          <a href="{{ path('app_register') }}" style="font-family: 'PT Sans', sans-serif;color:#0e6efd!important;" class="text-primary transition ">
            {{ 'app_login.register'|trans }}
          </a>
          {{ 'app_login.join'|trans }}
        </p>
          <form method="post">
            {% if error %}
              <div class="alert alert-dark">
                {{ error.messageKey|trans(error.messageData, 'security') }}
              </div>
            {% endif %}
            <div class="mx-auto text-dark " id="button-text">
              <a href="{{ path('connect_google_start') }}" class="btn btn-light rounded-pill px-5 ">
                {{ 'app_register.continue'|trans }}
                <i class="bi bi-google h5 mx-2 "></i>
              </a>
            </div>
            <hr>

              {% if app.user %}
                <div class="mb-3">
                  You are logged in as  {{ app.user.userIdentifier }},                                     
                  <a href="{{ path('app_logout') }}">
                    Logout                                      
                  </a>
                </div>
              {% endif %}

              <label for="inputEmail" class="mt-1">
                {{ 'app_login.email'|trans }}
              </label>
              <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                <label for="inputPassword" class="mt-3">
                  {{ 'app_login.password'|trans }}
                </label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                  <div class="my-3 form-check">
                    <div class="row">
                      <div class="col-6">
                        <input type="checkbox" class="form-check-input" id="exampleCheck1">
                          <label class="form-check-label" for="exampleCheck1">
                            {{ 'app_login.remember'|trans }}
                          </label>
                        </div>
                        <div class="col-6 text-right">
                          <!--Forgot password link-->
                          <a href="{{ path('app_forgot_password_request')}}" class="" style="font-family: 'PT Sans', sans-serif;color:#0e6efd!important;">
                            {{ 'app_forgot_password_request.link'|trans }}
                          </a>
                        </div>
                      </div>
                    </div>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                      <button class="btn rounded-pill btn-lg px-5 btn-primary" type="submit">
                        {{ "app_login.button"|trans }}
                      </button>
                    </form>
                  </div>
                </div>
          </div>
        </section>
      {% endblock %}
           #}
{% extends 'base_v21.html.twig' %}

{% block title %}Connexion | Olona Talents{% endblock %}

{% block meta %}
    {{ parent() }}
{% endblock %}

{% block body %}

<section class="d-flex justify-content-center align-items-center" style="min-height: 100vh; background-color: #f8f9fa;">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6 d-none d-md-block text-center">
                <img src="{{ asset('v2/images/31.png') }}" class="img-fluid" alt="Image" style="max-width: 400px;">
            </div>
            <div class="col-md-6 col-lg-5">
                <div class="card shadow-lg border-0 rounded">
                    <div class="card-body p-4">
                        <h2 class="text-center mb-4">Se connecter</h2>
                        <p class="text-center mb-4">
                            Pas encore membre ? <a href="{{ path('app_olona_talents_premium') }}" class="text-primary">Inscrivez-vous dès maintenant</a> et rejoignez la communauté Olona-talents.
                        </p>
                        <div class="mb-4 text-center">
                            <a href="{{ path('connect_google_start') }}" class="btn btn-outline-primary btn-block">
                                Se connecter avec <i class="bi bi-google"></i>
                            </a>
                        </div>
                        
                        {% if error %}
                        <div class="alert alert-danger">
                            {{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                        {% endif %}
                        
                        {% if app.user %}
                        <div class="alert alert-dark text-center">
                            Vous êtes connecté en tant que {{ app.user.userIdentifier }},
                            <a href="{{ path('app_logout') }}">Déconnexion</a>
                        </div>
                        {% endif %}
                        
                        <form method="post">
                            <div class="mb-3">
                                <input type="email" name="email" id="email" value="{{ last_username }}" class="form-control" placeholder="Adresse e-mail" required autofocus>
                            </div>

                            <div class="mb-3">
                                <input type="password" name="password" id="password" class="form-control" placeholder="Votre mot de passe" required>
                            </div>

                            <div class="form-check mb-3">
                                <input type="checkbox" class="form-check-input" id="terms">
                                <label class="form-check-label" for="terms">
                                    En vous connectant sur Olona Talents, vous acceptez nos conditions générales d'utilisation
                                </label>
                            </div>

                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                            
                            <button type="submit" class="btn btn-primary btn-block">Connexion</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}
